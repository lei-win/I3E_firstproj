using UnityEngine;

public class mybabyscript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Hello my baby boo World");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

